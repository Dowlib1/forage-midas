# Existing config might be here, e.g.:
# server:
#   port: 8080

general:
  kafka-topic: trader-updates

spring:
  kafka:
    producer:
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    # If you have a consumer in tests/code, add this too (adjust key.deserializer if needed):
    # consumer:
    #   value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
    #   properties:
    #     spring.json.trusted.packages: '*'  # Allows deserializing your package